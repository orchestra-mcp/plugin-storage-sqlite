---
created_at: "2026-02-28T02:51:21Z"
description: 'Convenience wrapper over OrchestraClient for typed MCP tool calls. Methods: list_projects(), get_project_status(slug), create_feature(project, title, type), advance_feature(id, evidence), ai_prompt(prompt, provider, model), spawn_session(id, prompt, provider). All methods async. Returns typed model objects parsed from JSON tool responses.'
id: FEAT-WKY
priority: P0
project_id: orchestra-linux
status: backlog
title: ToolService MCP tool call proxy
updated_at: "2026-02-28T02:51:21Z"
version: 0
---

# ToolService MCP tool call proxy

Convenience wrapper over OrchestraClient for typed MCP tool calls. Methods: list_projects(), get_project_status(slug), create_feature(project, title, type), advance_feature(id, evidence), ai_prompt(prompt, provider, model), spawn_session(id, prompt, provider). All methods async. Returns typed model objects parsed from JSON tool responses.
